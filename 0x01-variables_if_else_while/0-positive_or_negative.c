#include <stdio.h>
/**
 * main - Entry point
 * variable used - while loop
 * Return: Allow 0 (Success)
 */
if (n == 0)
{
	printf("%d is zero\n", n);
}
else if (n > 0)
{
	printf("%d is positive\n", n);
}
else
{
	printf("%d is negative\n", n);
}
