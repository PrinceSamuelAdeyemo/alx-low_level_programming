#include <stdio.h>
/*This is to output in C using printf
 * main is the beginning of the function
 * printf is the output statement
 * return ends the program*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
